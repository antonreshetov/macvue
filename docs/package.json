{
  "name": "@macvue/docs",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter macvue build:tokens && vitepress dev",
    "build": "pnpm --filter macvue build:tokens && vitepress build",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "macvue": "workspace:*"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.41"
  }
}
